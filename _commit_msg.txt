fix: 修复历史命令记录缺失/脏记录，恢复密码明文展示

- 前端回车恢复即时记录（兜底）+ 后端回显解析为主：
  record_echo_command 清理 3 秒内前缀残留（cd /va vs cd /var）并去重
- 提示符解析：循环剥离一行内多个提示符（阿里云 shell 清屏重绘），
  自定义 PS1 宽松兜底（行首到第一个 $/#），宽松失败返回空串
- 恢复密码明文展示：删除 show_password_plaintext 脱敏开关，
  后端明文下发（_sanitize_host 保留原值），前端密码框恢复明文，
  复制连接信息包含密码
- docs: 补录第 32 条教训（回显解析空白 + 多提示符脏记录），
  更新密码安全说明
