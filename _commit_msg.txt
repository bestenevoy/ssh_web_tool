fix: 连接时历史输出按错误宽度折行错乱；去掉无意义的恢复/加载提示

- 写入历史输出（banner/MOTD）前先等待容器就绪并 fit + 发送 resize
  （ensureFit 最多重试约 600ms）：历史内容按默认 80 列写入后，
  fit 到实际宽度已写入行不会重新折行，导致中间信息位置错乱
- 去掉终端内"[--- 历史输出加载完成 ---]"标记（无实际意义）
- 后端去掉 ws 连接时"已恢复已有终端会话"提示（恢复逻辑保留，
  恢复内容由前端历史输出直接体现）
