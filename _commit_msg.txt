fix: 关闭终端标签同时删除后端会话，恢复功能只针对真实存活的连接

- closeTerminal 去掉 closeBackend 参数，关闭标签一律删除后端会话
  （不再"仅关闭标签、后端连接保持"，避免重开页面恢复已关闭的会话）
- 恢复已有会话逻辑保留：仅当整关页面但程序未退、后端连接仍真实存活时，
  重开页面自动恢复（后端 get_active_terminals 已按 channel/进程存活过滤）
- 关闭标签确认弹窗文案更新；空状态提示同步更新
