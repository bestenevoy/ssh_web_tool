test: 建立可扩展测试框架 + 清理 param_hint 废弃字段

新增 pytest 测试体系（tests/，pytest + pytest-asyncio）：
- unit/：纯逻辑无 IO（storage 原子写、history_db、命令回显解析）
- api/：TestClient + 内存会话 mock（不连真实 SSH）
- e2e/：真实 SSH（默认 skip，--e2e 启用）
- conftest：import main 前隔离数据目录；client fixture 每测试全新数据文件
- run_tests.ps1 / pytest.ini / tests/README.md：一键运行与扩展说明

回归测试覆盖已修复的 4 个 Bug：
- 预操作上传二进制无损（含 e2e 真实 SSH 版）
- 注入命令并发锁（inject_and_capture 事务串行）
- 输出广播有界队列（丢最旧保最新，不阻塞广播链）
- data.json 原子写（无 .tmp 残留 + 崩溃容错）
- 顺带：script 路径穿越 400

清理 param_hint 废弃字段（前端 types 已删，storage/main 残留）：
- storage.add/update_quick_command 不再写该字段，DEFAULT 同步清理
- main.py QuickCommandRequest 移除字段、handler 不再透传

约定（写入 docs/development-lessons.md #33）：
以后修复任何 Bug 必须同步添加回归测试，全量测试通过才算修复完成

测试结果：47 passed + 3 e2e passed（真实 SSH 8.137.52.71）
